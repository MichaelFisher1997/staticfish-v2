---
const navigation = [
  { name: "Home", href: "/" },
  { name: "Services", href: "/services" },
  { name: "Pricing", href: "/pricing" },
  { name: "Portfolio", href: "/portfolio" },
  { name: "About", href: "/about" },
  { name: "Contact", href: "/contact" },
];

const legalLinks = [
  { name: "Privacy Policy", href: "/privacy" },
  { name: "Terms of Service", href: "/terms" },
];
---

<footer class="relative mt-32 border-t border-border/50">
  <!-- Subtle gradient line at top -->
  <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-primary/30 to-transparent"></div>
  
  <!-- Ambient glow -->
  <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[600px] h-[300px] bg-gradient-to-t from-ocean/5 to-transparent rounded-full blur-[100px] pointer-events-none"></div>

  <div class="container relative z-10 mx-auto max-w-7xl px-6 py-16">
    
    <!-- Main footer content -->
    <div class="flex flex-col lg:flex-row justify-between items-start gap-12 mb-16">
      
      <!-- Brand column -->
      <div class="max-w-xs">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-ocean to-primary flex items-center justify-center text-white shadow-md shadow-ocean/20">
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z" fill="currentColor" opacity="0.2"/>
              <path d="M12 4c-1.5 2-2.5 4-2.5 6s1 4 2.5 6c1.5-2 2.5-4 2.5-6s-1-4-2.5-6z" fill="currentColor"/>
            </svg>
          </div>
          <span class="text-xl font-bold text-ink font-display tracking-tight">
            staticfish
          </span>
        </div>
        <p class="text-sm text-muted-foreground leading-relaxed">
          Fast, secure websites for small businesses. No WordPress headaches, no hidden costs.
        </p>
      </div>

      <!-- Navigation column -->
      <div class="flex flex-wrap gap-x-16 gap-y-8">
        <div>
          <h4 class="text-xs font-bold uppercase tracking-widest text-ink mb-4">Navigation</h4>
          <nav class="flex flex-col gap-3">
            {navigation.slice(0, 3).map((item) => (
              <a
                href={item.href}
                class="text-sm text-muted-foreground hover:text-primary transition-colors duration-200"
              >
                {item.name}
              </a>
            ))}
          </nav>
        </div>
        <div>
          <h4 class="text-xs font-bold uppercase tracking-widest text-ink mb-4">&nbsp;</h4>
          <nav class="flex flex-col gap-3">
            {navigation.slice(3).map((item) => (
              <a
                href={item.href}
                class="text-sm text-muted-foreground hover:text-primary transition-colors duration-200"
              >
                {item.name}
              </a>
            ))}
          </nav>
        </div>
        <div>
          <h4 class="text-xs font-bold uppercase tracking-widest text-ink mb-4">Legal</h4>
          <nav class="flex flex-col gap-3">
            {legalLinks.map((item) => (
              <a
                href={item.href}
                class="text-sm text-muted-foreground hover:text-primary transition-colors duration-200"
              >
                {item.name}
              </a>
            ))}
          </nav>
        </div>
      </div>
    </div>
    
    <!-- Bottom bar -->
    <div class="pt-8 border-t border-border/50 flex flex-col sm:flex-row justify-between items-center gap-4">
      <p class="text-xs text-muted-foreground">
        © {new Date().getFullYear()} Staticfish. Operated by Michael Fisher in the UK.
      </p>
      <p class="text-xs text-muted-foreground/60">
        Professional websites at a fair price.
      </p>
    </div>
  </div>
</footer>
