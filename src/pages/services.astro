---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PageHero from '../components/PageHero.astro';
import CtaPanel from '../components/CtaPanel.astro';
import Reveal from '../components/effects/Reveal.astro';
import Steps from '../components/Steps.astro';
import { ArrowRight, Check, Clock, Gauge, MousePointerClick, ShieldCheck, Sparkles } from 'lucide-react';

const websitePackage = {
  priceGbp: 599,
  timeline: '1-3 weeks',
  features: [
    'The core pages your business needs to launch',
    'Custom design for your business',
    'Mobile-friendly layout',
    'Contact form',
    'Basic SEO setup',
    'Launch support'
  ],
  goodFit: [
    'Local service businesses',
    'Consultants and freelancers',
    'Studios, trades, and small teams'
  ]
};

const addOns = [
  {
    title: 'Editable Content Add-On',
    priceGbp: 800,
    description: 'Adds a CMS so you can update pages, services, pricing, testimonials, and publish blog or news posts yourself.',
    features: ['CMS setup', 'Editable page content and blog/news', 'Editor training at handover'],
    featured: true,
  }
];

const processSteps = [
  {
    marker: '01',
    title: 'Quick Discovery',
    description: 'A short call or email exchange to understand your business and what the website needs to do.',
    meta: '30-60 min'
  },
  {
    marker: '02',
    title: 'Direction First',
    description: 'I put together the draft direction or an initial page so you can see whether the look and structure feel right.',
    meta: '1-2 days'
  },
  {
    marker: '03',
    title: 'Build and Review',
    description: 'Once the direction is approved, I complete the site and we review everything together before launch.',
    meta: '1-3 weeks'
  },
  {
    marker: '04',
    title: 'Payment, Domain, and Launch',
    description: 'When the site is approved, payment is made before launch. I then handle DNS, domain setup, and getting the site live.',
    meta: '1-2 days'
  }
];

const whyPoints = [
  { title: "Clear information first", desc: "Visitors can quickly understand what you do, what it costs, and what to do next.", icon: MousePointerClick },
  { title: "Fast and low-maintenance", desc: "Simple websites tend to be quicker to launch, easier to run, and less likely to create technical headaches.", icon: Gauge },
  { title: "Editing only when needed", desc: "If your prices, services, or updates change regularly, the editing add-on keeps that manageable without overcomplicating the whole site.", icon: ShieldCheck },
];
---

<Layout title="Websites | Staticfish" description="Clear business websites for small businesses, with an optional CMS setup if you want to manage content yourself.">
  <div class="min-h-screen flex flex-col">
    <Header />
    <main class="flex-1">
      <PageHero eyebrow="Websites">
        <span slot="title">Websites for <span class="gradient-text-bright">small businesses.</span></span>
        The main offer at Staticfish is a clear business website. If you want to update content yourself, I can add a proper CMS rather than forcing that complexity into every project.
      </PageHero>

      <!-- Core package -->
      <section class="mx-auto max-w-6xl px-6 py-16 lg:py-24">
        <Reveal variant="scale">
          <div class="overflow-hidden rounded-[2rem] border border-white/10 bg-white/[0.03] backdrop-blur">
            <div class="grid lg:grid-cols-[1fr_1.15fr]">
              <div class="p-8 sm:p-10 lg:p-12">
                <div class="flex flex-wrap items-center gap-2">
                  <span class="inline-flex items-center gap-1.5 rounded-full border border-white/15 bg-white/5 px-3.5 py-1.5 text-[11px] font-bold uppercase tracking-widest text-slate-200">
                    <Clock className="h-3 w-3" /> {websitePackage.timeline}
                  </span>
                  <span class="inline-flex items-center gap-1.5 rounded-full border border-emerald-400/30 bg-emerald-400/10 px-3.5 py-1.5 text-[11px] font-bold uppercase tracking-widest text-emerald-300">
                    <span class="live-badge inline-block h-1.5 w-1.5 rounded-full bg-emerald-400"></span> Booking now
                  </span>
                </div>
                <h2 class="mt-6 font-display text-3xl font-bold tracking-tight text-white">Business Website</h2>
                <p class="mt-3 leading-relaxed text-slate-400">
                  A professional website built to explain who you are, what you offer, what it costs, and how people can contact you.
                </p>
                <p class="mt-8 flex items-baseline gap-2">
                  <span class="font-display text-6xl font-bold tracking-tight text-white" data-price-gbp={websitePackage.priceGbp}>£{websitePackage.priceGbp}</span>
                  <span class="text-sm font-medium text-slate-500">upfront</span>
                </p>
                <p class="mt-1.5 text-sm text-slate-500" data-hosting-gbp>+ £25/month hosting, backups &amp; upkeep</p>
                <div class="mt-8 flex flex-col gap-3 sm:flex-row lg:flex-col xl:flex-row">
                  <a href="/contact" class="btn-primary-glow group inline-flex items-center justify-center gap-2 rounded-full px-7 py-3.5 text-sm font-bold text-white">
                    <span class="relative z-[2] flex items-center gap-2">Ask for a Quote <ArrowRight className="h-4 w-4 transition-transform duration-300 group-hover:translate-x-1" /></span>
                  </a>
                  <a href="/pricing" class="btn-ghost-glass inline-flex items-center justify-center rounded-full px-7 py-3.5 text-sm font-bold">
                    Compare pricing
                  </a>
                </div>
              </div>
              <div class="border-t border-white/10 bg-white/[0.02] p-8 sm:p-10 lg:border-l lg:border-t-0 lg:p-12">
                <p class="mb-2 text-[11px] font-bold uppercase tracking-[0.18em] text-slate-500">What's included</p>
                <ul class="divide-y divide-white/10">
                  {websitePackage.features.map((feature) => (
                    <li class="flex items-center gap-3 py-3.5">
                      <Check className="h-4 w-4 shrink-0 text-emerald-400" strokeWidth={3} />
                      <span class="text-[15px] font-medium text-slate-200">{feature}</span>
                    </li>
                  ))}
                </ul>
                <p class="mb-3 mt-8 text-[11px] font-bold uppercase tracking-[0.18em] text-slate-500">Good fit for</p>
                <div class="flex flex-wrap gap-2">
                  {websitePackage.goodFit.map((item) => (
                    <span class="rounded-full border border-white/15 bg-white/5 px-3.5 py-1.5 text-xs font-semibold text-slate-300">
                      {item}
                    </span>
                  ))}
                </div>
                <p class="mt-6 border-t border-white/10 pt-6 text-sm leading-relaxed text-slate-400">
                  If you need editable content, those projects start from <strong class="font-semibold text-white" data-currency-gbp="50" data-currency-suffix="/month">£50/month</strong> ongoing to cover the CMS, hosting, and support properly.
                </p>
              </div>
            </div>
          </div>
        </Reveal>
      </section>

      <!-- Add-on -->
      <section class="border-y border-white/[0.07] bg-white/[0.02] py-16 lg:py-24">
        <div class="mx-auto max-w-6xl px-6">
          <div class="mb-10 max-w-2xl">
            <Reveal>
              <span class="eyebrow-pill"><span class="eyebrow-dot"></span> Optional add-on</span>
            </Reveal>
            <Reveal delay={90}>
              <h2 class="mt-5 font-display text-3xl font-bold tracking-tight text-white sm:text-4xl">Add editing <span class="serif-accent font-normal">if you need it.</span></h2>
            </Reveal>
            <Reveal delay={150}>
              <p class="mt-4 leading-relaxed text-slate-400">
                Most sites do not need a CMS. If you want to edit pages, services, pricing, testimonials, or publish blog and news posts yourself, I can add one cleanly.
              </p>
            </Reveal>
          </div>

          {addOns.map((addOn) => (
            <Reveal variant="scale">
              <div class="max-w-3xl rounded-[1.75rem] border border-white/10 bg-white/[0.03] p-8 backdrop-blur sm:p-10">
                {addOn.featured && (
                  <span class="mb-5 inline-flex items-center gap-1.5 rounded-full bg-white px-3.5 py-1.5 text-[11px] font-bold uppercase tracking-widest text-slate-900">
                    <Sparkles className="h-3 w-3" /> Most common add-on
                  </span>
                )}
                <div class="flex flex-wrap items-end justify-between gap-6">
                  <div class="max-w-md">
                    <h3 class="font-display text-2xl font-bold tracking-tight text-white">{addOn.title}</h3>
                    <p class="mt-3 leading-relaxed text-slate-400">{addOn.description}</p>
                  </div>
                  <p class="text-right">
                    <span class="block text-[11px] font-bold uppercase tracking-widest text-slate-500">from +</span>
                    <span class="font-display text-5xl font-bold tracking-tight text-white" data-price-gbp={addOn.priceGbp}>£{addOn.priceGbp}</span>
                  </p>
                </div>
                <ul class="mt-7 flex flex-wrap gap-x-8 gap-y-3 border-t border-white/10 pt-7">
                  {addOn.features.map((feature) => (
                    <li class="flex items-center gap-2.5 text-sm font-medium text-slate-300">
                      <Check className="h-4 w-4 shrink-0 text-emerald-400" strokeWidth={3} />
                      {feature}
                    </li>
                  ))}
                </ul>
              </div>
            </Reveal>
          ))}

          <Reveal delay={100}>
            <div class="mt-8 max-w-3xl space-y-4 text-sm leading-relaxed text-slate-400">
              <p>
                Editable-content projects also have a higher ongoing fee than brochure sites, because there is a real CMS and editor access to support. If you need a more tailored enquiry flow, booking setup, or something outside the normal website scope, I quote that separately based on what the business actually needs.
              </p>
              <p>
                If you already have a domain name, I can connect it. If you need a new one, I can register it for you, but the domain cost is paid by you separately.
              </p>
            </div>
          </Reveal>
        </div>
      </section>

      <!-- Why -->
      <section class="mx-auto max-w-6xl px-6 py-16 lg:py-24">
        <div class="mb-12 max-w-2xl">
          <Reveal>
            <span class="eyebrow-pill"><span class="eyebrow-dot"></span> Why this works</span>
          </Reveal>
          <Reveal delay={90}>
            <h2 class="mt-5 font-display text-3xl font-bold tracking-tight text-white sm:text-4xl">
              Built to be easy to understand.
            </h2>
          </Reveal>
          <Reveal delay={150}>
            <p class="mt-4 leading-relaxed text-slate-400">
              Most small businesses do not need a sprawling platform. They need a site that feels professional, loads quickly, and gives visitors the information they need without friction.
            </p>
          </Reveal>
        </div>
        <div class="grid gap-10 sm:grid-cols-3 sm:gap-0 sm:divide-x sm:divide-white/10">
          {whyPoints.map((point, i) => (
            <div data-reveal style={`--reveal-delay: ${i * 100}ms`} class="sm:px-8 sm:first:pl-0 sm:last:pr-0">
              <span class="inline-flex h-10 w-10 items-center justify-center rounded-xl border border-white/10 bg-white/[0.06] text-white">
                <point.icon className="h-[18px] w-[18px]" />
              </span>
              <h3 class="mt-5 font-display text-lg font-bold tracking-tight text-white">{point.title}</h3>
              <p class="mt-2 text-sm leading-relaxed text-slate-400">{point.desc}</p>
            </div>
          ))}
        </div>
      </section>

      <!-- Process -->
      <section class="mx-auto max-w-4xl px-6 pb-20 lg:pb-28">
        <Reveal>
          <span class="eyebrow-pill"><span class="eyebrow-dot"></span> Process</span>
        </Reveal>
        <Reveal delay={90}>
          <h2 class="mb-10 mt-5 font-display text-3xl font-bold tracking-tight text-white sm:text-4xl">A simple process.</h2>
        </Reveal>
        <Steps items={processSteps} />
      </section>

      <CtaPanel
        eyebrow="Secondary service"
        title="Need something more"
        highlight="custom?"
        description="Most of my work is business websites, but I also take on selected custom software projects when a business needs a portal, dashboard, workflow, or bespoke web app."
        primary={{ href: "/custom-software", label: "View Custom Software" }}
        secondary={{ href: "/contact", label: "Ask About Your Project" }}
      />
    </main>
    <Footer />
  </div>
</Layout>
